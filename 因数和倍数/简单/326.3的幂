题目描述：
给定一个整数，写一个函数来判断它是否是 3 的幂次方。如果是，返回 true ；否则，返回 false 。
整数 n 是 3 的幂次方需满足：存在整数 x 使得 n == 3^x
示例 1：
输入：n = 27
输出：true
示例 2：
输入：n = 0
输出：false
示例 3：
输入：n = 9
输出：true
示例 4：
输入：n = 45
输出：false
提示：
-2^31 <= n <= 2^31 - 1

做题时间：2026.4.19
思考：（查阅题解后）在-2^31 <= n <= 2^31 - 1范围内，3的幂能取到的最大值是
3^19 = 1,162,261,467.00
2^31-1 = 2,147,483,647.99
所以，在这个范围内，如果是3的幂次数就一定是3^19的因数。

题解：
class Solution {
public:
    bool isPowerOfThree(int n) {
        if(n>=1){
            if(int(pow(3,19)) % n == 0) return true;
            else return false;
        }
        else return false;
    }
};

做题时间：2026.4.21
思考：因为今天学java上基础：while循环的时候，做到质数的题，所以想再做做看，但还是不会。
（查阅题解后）
进入while循环的条件：n不为零且是3的倍数

题解：
class Solution {
public:
    bool isPowerOfThree(int n) {
        while(n && n % 3 == 0){
            n /= 3;
        }
        return n == 1;
    }
};
